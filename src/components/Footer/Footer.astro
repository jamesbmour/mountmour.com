---
import AvatarList from './AvatarList.astro'
type Props = {
  path: string
}
const { path } = Astro.props
---

<footer class="mt-auto p-8 border-t-4 border-theme-divider">
  <AvatarList path={path} />
</footer>