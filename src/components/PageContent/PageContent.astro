---
import type { MarkdownHeading } from "astro";
import MoreMenu from "../RightSidebar/MoreMenu.astro";
import TableOfContents from "../RightSidebar/TableOfContents";

type Props = {
  title: string;
  headings: MarkdownHeading[];
  editUrl: string;
};

const { title, headings, editUrl } = Astro.props;
---

<!-- TODO: Need to make the maps larger if no text is present -->
<article id="article" class="p-0 w-full h-full flex flex-col pt-4">
  <section class="mb-12">
    <h1 class="text-2xl font-bold" id="overview">{title}</h1>
    <nav class="block sm:hidden">
      <TableOfContents client:media="(max-width: 40em)" headings={headings} />
    </nav>
    <slot />
  </section>
  <nav class="block sm:hidden">
    <MoreMenu editHref={editUrl} />
  </nav>
</article>
